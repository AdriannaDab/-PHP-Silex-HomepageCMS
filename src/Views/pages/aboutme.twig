{% extends 'layout.twig' %}

{% block content %}
    <hr>
    <h2 class="intro-text text-center">
        O mnie
    </h2>
    <hr>
    {% for page in pages %}
        <p>{{ page.title|e }} : {{ page.content|raw }}</p>
    {% endfor %}
{% endblock %}