{% extends 'layout.twig' %}

{% block content %}
    <hr>
    <h2 class="intro-text text-center">
        Kontakt
    </h2>
    <hr>
    {% for page in pages %}
        <p>{{ page.title|trans }} : {{ page.content|raw }}</p>
    {% endfor %}
{% endblock %}