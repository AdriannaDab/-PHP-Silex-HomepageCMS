{% extends 'layout.twig' %}

{% block content %}
    <hr>
    <h2 class="intro-text text-center">
        Dodaj zdjęcie
    </h2>
    <hr>
    <form action="{{ url('/photos/upload', {'idproject': idproject|e}) }}" {{ form_enctype(form) }}
          method="post">
        {{ form_widget(form) }}
    </form>
{% endblock %}