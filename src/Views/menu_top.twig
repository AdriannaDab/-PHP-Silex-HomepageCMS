<div class="container">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#menu_collapse_2">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <span class="navbar-brand">Control Panel</span>
    </div>
    <div class="collapse navbar-collapse navbar-ex1-collapse" id="menu_collapse_2">
        <ul class="nav navbar-nav">
            {% if is_granted('ROLE_ADMIN') %}
                <li class="dropdown">
                    <a href="#" data-toggle="dropdown" class="dropdown-toggle">
                        Edytor tekstów <b class="caret"></b></a>
                    <ul class="dropdown-menu">
                        <li>
                            <a href="{{ path('/posts/add') }}">Dodaj post</a>
                        </li>
                        <li>
                            <a href="{{ path('/projects/add') }}">Dodaj projekt</a>
                        </li>
                        <li><a href="{{ path('/pages/editaboutme') }}">O mnie</a>
                        </li>
                        <li><a href="{{ path('/pages/editcontact') }}">Kontakt</a>
                        </li>
                    </ul>
                <li>
                <li class="dropdown">
                    <a href="#" data-toggle="dropdown" class="dropdown-toggle">
                        Control panel <b class="caret"></b></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('/tags/controlpanel') }}">Manager Tagów</a>
                        </li>
                        <li><a href="{{ path('/categories/controlpanel') }}">Manager
                                kategorii</a></li>
                        <li><a href="{{ path('/photos/manager') }}">Manager zdjeć</a>
                        </li>
                    </ul>
                </li>
            {% endif %}
            <li class="dropdown">
                <a href="#" data-toggle="dropdown" class="dropdown-toggle"> Ustawienia
                    <b class="caret"></b></a>
                <ul class="dropdown-menu">
                    <li><a href="{{ path('/users/view') }}">Profil</a></li>
                    <li><a href="{{ path('/users/edit') }}">Edytuj profil</a></li>
                    <li><a href="{{ path('/users/password') }}">Zmień hasło</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                    {% else %}
                        <li><a href="{{ path('/users/delete') }}">Usuń konto</a></li>
                    {% endif %}
                    <li>
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            <a href="{{ path('/auth/logout') }}">Wyloguj</a>
                        {% endif %}
                    </li>
                </ul>
            </li>
        </ul>
    </div>
</div>
